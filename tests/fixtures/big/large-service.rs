pub struct OrderService;

impl OrderService {
    pub fn process_order(&self, qty: i32, price: i32) -> i32 {
        let mut subtotal = 0;
        subtotal += qty * price + 1;
        subtotal += qty * price + 2;
        subtotal += qty * price + 3;
        subtotal += qty * price + 4;
        subtotal += qty * price + 5;
        subtotal += qty * price + 6;
        subtotal += qty * price + 7;
        subtotal += qty * price + 8;
        subtotal += qty * price + 9;
        subtotal += qty * price + 10;
        subtotal += qty * price + 11;
        subtotal += qty * price + 12;
        subtotal += qty * price + 13;
        subtotal += qty * price + 14;
        subtotal += qty * price + 15;
        subtotal += qty * price + 16;
        subtotal += qty * price + 17;
        subtotal += qty * price + 18;
        subtotal += qty * price + 19;
        subtotal += qty * price + 20;
        subtotal += qty * price + 21;
        subtotal += qty * price + 22;
        subtotal += qty * price + 23;
        subtotal += qty * price + 24;
        subtotal += qty * price + 25;
        subtotal += qty * price + 26;
        subtotal += qty * price + 27;
        subtotal += qty * price + 28;
        subtotal += qty * price + 29;
        subtotal += qty * price + 30;
        subtotal += qty * price + 31;
        subtotal += qty * price + 32;
        subtotal += qty * price + 33;
        subtotal += qty * price + 34;
        subtotal += qty * price + 35;
        subtotal += qty * price + 36;
        subtotal += qty * price + 37;
        subtotal += qty * price + 38;
        subtotal += qty * price + 39;
        subtotal += qty * price + 40;
        subtotal += qty * price + 41;
        subtotal += qty * price + 42;
        subtotal += qty * price + 43;
        subtotal += qty * price + 44;
        subtotal += qty * price + 45;
        subtotal += qty * price + 46;
        subtotal += qty * price + 47;
        subtotal += qty * price + 48;
        subtotal += qty * price + 49;
        subtotal += qty * price + 50;
        subtotal += qty * price + 51;
        subtotal += qty * price + 52;
        subtotal += qty * price + 53;
        subtotal += qty * price + 54;
        subtotal += qty * price + 55;
        subtotal += qty * price + 56;
        subtotal += qty * price + 57;
        subtotal += qty * price + 58;
        subtotal += qty * price + 59;
        subtotal += qty * price + 60;
        subtotal += qty * price + 61;
        subtotal += qty * price + 62;
        subtotal += qty * price + 63;
        subtotal += qty * price + 64;
        subtotal += qty * price + 65;
        subtotal += qty * price + 66;
        subtotal += qty * price + 67;
        subtotal += qty * price + 68;
        subtotal += qty * price + 69;
        subtotal += qty * price + 70;
        subtotal += qty * price + 71;
        subtotal += qty * price + 72;
        subtotal += qty * price + 73;
        subtotal += qty * price + 74;
        subtotal += qty * price + 75;
        subtotal += qty * price + 76;
        subtotal += qty * price + 77;
        subtotal += qty * price + 78;
        subtotal += qty * price + 79;
        subtotal += qty * price + 80;
        subtotal += qty * price + 81;
        subtotal += qty * price + 82;
        subtotal += qty * price + 83;
        subtotal += qty * price + 84;
        subtotal += qty * price + 85;
        subtotal += qty * price + 86;
        subtotal += qty * price + 87;
        subtotal += qty * price + 88;
        subtotal += qty * price + 89;
        subtotal += qty * price + 90;
        subtotal += qty * price + 91;
        subtotal += qty * price + 92;
        subtotal += qty * price + 93;
        subtotal += qty * price + 94;
        subtotal += qty * price + 95;
        subtotal += qty * price + 96;
        subtotal += qty * price + 97;
        subtotal += qty * price + 98;
        subtotal += qty * price + 99;
        subtotal += qty * price + 100;
        subtotal += qty * price + 101;
        subtotal += qty * price + 102;
        subtotal += qty * price + 103;
        subtotal += qty * price + 104;
        subtotal += qty * price + 105;
        subtotal += qty * price + 106;
        subtotal += qty * price + 107;
        subtotal += qty * price + 108;
        subtotal += qty * price + 109;
        subtotal += qty * price + 110;
        subtotal += qty * price + 111;
        subtotal += qty * price + 112;
        subtotal += qty * price + 113;
        subtotal += qty * price + 114;
        subtotal += qty * price + 115;
        subtotal += qty * price + 116;
        subtotal += qty * price + 117;
        subtotal += qty * price + 118;
        subtotal += qty * price + 119;
        subtotal += qty * price + 120;
        subtotal += qty * price + 121;
        subtotal += qty * price + 122;
        subtotal += qty * price + 123;
        subtotal += qty * price + 124;
        subtotal += qty * price + 125;
        subtotal += qty * price + 126;
        subtotal += qty * price + 127;
        subtotal += qty * price + 128;
        subtotal += qty * price + 129;
        subtotal += qty * price + 130;
        subtotal += qty * price + 131;
        subtotal += qty * price + 132;
        subtotal += qty * price + 133;
        subtotal += qty * price + 134;
        subtotal += qty * price + 135;
        subtotal += qty * price + 136;
        subtotal += qty * price + 137;
        subtotal += qty * price + 138;
        subtotal += qty * price + 139;
        subtotal += qty * price + 140;
        subtotal += qty * price + 141;
        subtotal += qty * price + 142;
        subtotal += qty * price + 143;
        subtotal += qty * price + 144;
        subtotal += qty * price + 145;
        subtotal += qty * price + 146;
        subtotal += qty * price + 147;
        subtotal += qty * price + 148;
        subtotal += qty * price + 149;
        subtotal += qty * price + 150;
        subtotal += qty * price + 151;
        subtotal += qty * price + 152;
        subtotal += qty * price + 153;
        subtotal += qty * price + 154;
        subtotal += qty * price + 155;
        subtotal += qty * price + 156;
        subtotal += qty * price + 157;
        subtotal += qty * price + 158;
        subtotal += qty * price + 159;
        subtotal += qty * price + 160;
        subtotal += qty * price + 161;
        subtotal += qty * price + 162;
        subtotal += qty * price + 163;
        subtotal += qty * price + 164;
        subtotal += qty * price + 165;
        subtotal += qty * price + 166;
        subtotal += qty * price + 167;
        subtotal += qty * price + 168;
        subtotal += qty * price + 169;
        subtotal += qty * price + 170;
        subtotal += qty * price + 171;
        subtotal += qty * price + 172;
        subtotal += qty * price + 173;
        subtotal += qty * price + 174;
        subtotal += qty * price + 175;
        subtotal += qty * price + 176;
        subtotal += qty * price + 177;
        subtotal += qty * price + 178;
        subtotal += qty * price + 179;
        subtotal += qty * price + 180;
        subtotal += qty * price + 181;
        subtotal += qty * price + 182;
        subtotal += qty * price + 183;
        subtotal += qty * price + 184;
        subtotal += qty * price + 185;
        subtotal += qty * price + 186;
        subtotal += qty * price + 187;
        subtotal += qty * price + 188;
        subtotal += qty * price + 189;
        subtotal += qty * price + 190;
        subtotal += qty * price + 191;
        subtotal += qty * price + 192;
        subtotal += qty * price + 193;
        subtotal += qty * price + 194;
        subtotal += qty * price + 195;
        subtotal += qty * price + 196;
        subtotal += qty * price + 197;
        subtotal += qty * price + 198;
        subtotal += qty * price + 199;
        subtotal += qty * price + 200;
        subtotal += qty * price + 201;
        subtotal += qty * price + 202;
        subtotal += qty * price + 203;
        subtotal += qty * price + 204;
        subtotal += qty * price + 205;
        subtotal += qty * price + 206;
        subtotal += qty * price + 207;
        subtotal += qty * price + 208;
        subtotal += qty * price + 209;
        subtotal += qty * price + 210;
        subtotal += qty * price + 211;
        subtotal += qty * price + 212;
        subtotal += qty * price + 213;
        subtotal += qty * price + 214;
        subtotal += qty * price + 215;
        subtotal += qty * price + 216;
        subtotal += qty * price + 217;
        subtotal += qty * price + 218;
        subtotal += qty * price + 219;
        subtotal += qty * price + 220;
        subtotal += qty * price + 221;
        subtotal += qty * price + 222;
        subtotal += qty * price + 223;
        subtotal += qty * price + 224;
        subtotal += qty * price + 225;
        subtotal += qty * price + 226;
        subtotal += qty * price + 227;
        subtotal += qty * price + 228;
        subtotal += qty * price + 229;
        subtotal += qty * price + 230;
        subtotal += qty * price + 231;
        subtotal += qty * price + 232;
        subtotal += qty * price + 233;
        subtotal += qty * price + 234;
        subtotal += qty * price + 235;
        subtotal += qty * price + 236;
        subtotal += qty * price + 237;
        subtotal += qty * price + 238;
        subtotal += qty * price + 239;
        subtotal += qty * price + 240;
        subtotal += qty * price + 241;
        subtotal += qty * price + 242;
        subtotal += qty * price + 243;
        subtotal += qty * price + 244;
        subtotal += qty * price + 245;
        subtotal += qty * price + 246;
        subtotal += qty * price + 247;
        subtotal += qty * price + 248;
        subtotal += qty * price + 249;
        subtotal += qty * price + 250;
        subtotal += qty * price + 251;
        subtotal += qty * price + 252;
        subtotal += qty * price + 253;
        subtotal += qty * price + 254;
        subtotal += qty * price + 255;
        subtotal += qty * price + 256;
        subtotal += qty * price + 257;
        subtotal += qty * price + 258;
        subtotal += qty * price + 259;
        subtotal += qty * price + 260;
        subtotal += qty * price + 261;
        subtotal += qty * price + 262;
        subtotal += qty * price + 263;
        subtotal += qty * price + 264;
        subtotal += qty * price + 265;
        subtotal += qty * price + 266;
        subtotal += qty * price + 267;
        subtotal += qty * price + 268;
        subtotal += qty * price + 269;
        subtotal += qty * price + 270;
        subtotal += qty * price + 271;
        subtotal += qty * price + 272;
        subtotal += qty * price + 273;
        subtotal += qty * price + 274;
        subtotal += qty * price + 275;
        subtotal += qty * price + 276;
        subtotal += qty * price + 277;
        subtotal += qty * price + 278;
        subtotal += qty * price + 279;
        subtotal += qty * price + 280;
        subtotal += qty * price + 281;
        subtotal += qty * price + 282;
        subtotal += qty * price + 283;
        subtotal += qty * price + 284;
        subtotal += qty * price + 285;
        subtotal += qty * price + 286;
        subtotal += qty * price + 287;
        subtotal += qty * price + 288;
        subtotal += qty * price + 289;
        subtotal += qty * price + 290;
        subtotal += qty * price + 291;
        subtotal += qty * price + 292;
        subtotal += qty * price + 293;
        subtotal += qty * price + 294;
        subtotal += qty * price + 295;
        subtotal += qty * price + 296;
        subtotal += qty * price + 297;
        subtotal += qty * price + 298;
        subtotal += qty * price + 299;
        subtotal += qty * price + 300;
        subtotal += qty * price + 301;
        subtotal += qty * price + 302;
        subtotal += qty * price + 303;
        subtotal += qty * price + 304;
        subtotal += qty * price + 305;
        subtotal += qty * price + 306;
        subtotal += qty * price + 307;
        subtotal += qty * price + 308;
        subtotal += qty * price + 309;
        subtotal += qty * price + 310;
        subtotal += qty * price + 311;
        subtotal += qty * price + 312;
        subtotal += qty * price + 313;
        subtotal += qty * price + 314;
        subtotal += qty * price + 315;
        subtotal += qty * price + 316;
        subtotal += qty * price + 317;
        subtotal += qty * price + 318;
        subtotal += qty * price + 319;
        subtotal += qty * price + 320;
        subtotal += qty * price + 321;
        subtotal += qty * price + 322;
        subtotal += qty * price + 323;
        subtotal += qty * price + 324;
        subtotal += qty * price + 325;
        subtotal += qty * price + 326;
        subtotal += qty * price + 327;
        subtotal += qty * price + 328;
        subtotal += qty * price + 329;
        subtotal += qty * price + 330;
        subtotal += qty * price + 331;
        subtotal += qty * price + 332;
        subtotal += qty * price + 333;
        subtotal += qty * price + 334;
        subtotal += qty * price + 335;
        subtotal += qty * price + 336;
        subtotal += qty * price + 337;
        subtotal += qty * price + 338;
        subtotal += qty * price + 339;
        subtotal += qty * price + 340;
        subtotal += qty * price + 341;
        subtotal += qty * price + 342;
        subtotal += qty * price + 343;
        subtotal += qty * price + 344;
        subtotal += qty * price + 345;
        subtotal += qty * price + 346;
        subtotal += qty * price + 347;
        subtotal += qty * price + 348;
        subtotal += qty * price + 349;
        subtotal += qty * price + 350;
        subtotal += qty * price + 351;
        subtotal += qty * price + 352;
        subtotal += qty * price + 353;
        subtotal += qty * price + 354;
        subtotal += qty * price + 355;
        subtotal += qty * price + 356;
        subtotal += qty * price + 357;
        subtotal += qty * price + 358;
        subtotal += qty * price + 359;
        subtotal += qty * price + 360;
        subtotal += qty * price + 361;
        subtotal += qty * price + 362;
        subtotal += qty * price + 363;
        subtotal += qty * price + 364;
        subtotal += qty * price + 365;
        subtotal += qty * price + 366;
        subtotal += qty * price + 367;
        subtotal += qty * price + 368;
        subtotal += qty * price + 369;
        subtotal += qty * price + 370;
        subtotal += qty * price + 371;
        subtotal += qty * price + 372;
        subtotal += qty * price + 373;
        subtotal += qty * price + 374;
        subtotal += qty * price + 375;
        subtotal += qty * price + 376;
        subtotal += qty * price + 377;
        subtotal += qty * price + 378;
        subtotal += qty * price + 379;
        subtotal += qty * price + 380;
        subtotal += qty * price + 381;
        subtotal += qty * price + 382;
        subtotal += qty * price + 383;
        subtotal += qty * price + 384;
        subtotal += qty * price + 385;
        subtotal += qty * price + 386;
        subtotal += qty * price + 387;
        subtotal += qty * price + 388;
        subtotal += qty * price + 389;
        subtotal += qty * price + 390;
        subtotal += qty * price + 391;
        subtotal += qty * price + 392;
        subtotal += qty * price + 393;
        subtotal += qty * price + 394;
        subtotal += qty * price + 395;
        subtotal += qty * price + 396;
        subtotal += qty * price + 397;
        subtotal += qty * price + 398;
        subtotal += qty * price + 399;
        subtotal += qty * price + 400;
        subtotal += qty * price + 401;
        subtotal += qty * price + 402;
        subtotal += qty * price + 403;
        subtotal += qty * price + 404;
        subtotal += qty * price + 405;
        subtotal += qty * price + 406;
        subtotal += qty * price + 407;
        subtotal += qty * price + 408;
        subtotal += qty * price + 409;
        subtotal += qty * price + 410;
        subtotal += qty * price + 411;
        subtotal += qty * price + 412;
        subtotal += qty * price + 413;
        subtotal += qty * price + 414;
        subtotal += qty * price + 415;
        subtotal += qty * price + 416;
        subtotal += qty * price + 417;
        subtotal += qty * price + 418;
        subtotal += qty * price + 419;
        subtotal += qty * price + 420;
        subtotal += qty * price + 421;
        subtotal += qty * price + 422;
        subtotal += qty * price + 423;
        subtotal += qty * price + 424;
        subtotal += qty * price + 425;
        subtotal += qty * price + 426;
        subtotal += qty * price + 427;
        subtotal += qty * price + 428;
        subtotal += qty * price + 429;
        subtotal += qty * price + 430;
        subtotal += qty * price + 431;
        subtotal += qty * price + 432;
        subtotal += qty * price + 433;
        subtotal += qty * price + 434;
        subtotal += qty * price + 435;
        subtotal += qty * price + 436;
        subtotal += qty * price + 437;
        subtotal += qty * price + 438;
        subtotal += qty * price + 439;
        subtotal += qty * price + 440;
        subtotal += qty * price + 441;
        subtotal += qty * price + 442;
        subtotal += qty * price + 443;
        subtotal += qty * price + 444;
        subtotal += qty * price + 445;
        subtotal += qty * price + 446;
        subtotal += qty * price + 447;
        subtotal += qty * price + 448;
        subtotal += qty * price + 449;
        subtotal += qty * price + 450;
        subtotal += qty * price + 451;
        subtotal += qty * price + 452;
        subtotal += qty * price + 453;
        subtotal += qty * price + 454;
        subtotal += qty * price + 455;
        subtotal += qty * price + 456;
        subtotal += qty * price + 457;
        subtotal += qty * price + 458;
        subtotal += qty * price + 459;
        subtotal += qty * price + 460;
        subtotal += qty * price + 461;
        subtotal += qty * price + 462;
        subtotal += qty * price + 463;
        subtotal += qty * price + 464;
        subtotal += qty * price + 465;
        subtotal += qty * price + 466;
        subtotal += qty * price + 467;
        subtotal += qty * price + 468;
        subtotal += qty * price + 469;
        subtotal += qty * price + 470;
        subtotal += qty * price + 471;
        subtotal += qty * price + 472;
        subtotal += qty * price + 473;
        subtotal += qty * price + 474;
        subtotal += qty * price + 475;
        subtotal += qty * price + 476;
        subtotal += qty * price + 477;
        subtotal += qty * price + 478;
        subtotal += qty * price + 479;
        subtotal += qty * price + 480;
        subtotal += qty * price + 481;
        subtotal += qty * price + 482;
        subtotal += qty * price + 483;
        subtotal += qty * price + 484;
        subtotal += qty * price + 485;
        subtotal += qty * price + 486;
        subtotal += qty * price + 487;
        subtotal += qty * price + 488;
        subtotal += qty * price + 489;
        subtotal += qty * price + 490;
        subtotal += qty * price + 491;
        subtotal += qty * price + 492;
        subtotal += qty * price + 493;
        subtotal += qty * price + 494;
        subtotal += qty * price + 495;
        subtotal += qty * price + 496;
        subtotal += qty * price + 497;
        subtotal += qty * price + 498;
        subtotal += qty * price + 499;
        subtotal += qty * price + 500;
        subtotal += qty * price + 501;
        subtotal += qty * price + 502;
        subtotal += qty * price + 503;
        subtotal += qty * price + 504;
        subtotal += qty * price + 505;
        subtotal += qty * price + 506;
        subtotal += qty * price + 507;
        subtotal += qty * price + 508;
        subtotal += qty * price + 509;
        subtotal += qty * price + 510;
        subtotal += qty * price + 511;
        subtotal += qty * price + 512;
        subtotal += qty * price + 513;
        subtotal += qty * price + 514;
        subtotal += qty * price + 515;
        subtotal += qty * price + 516;
        subtotal += qty * price + 517;
        subtotal += qty * price + 518;
        subtotal += qty * price + 519;
        subtotal += qty * price + 520;
        subtotal += qty * price + 521;
        subtotal += qty * price + 522;
        subtotal += qty * price + 523;
        subtotal += qty * price + 524;
        subtotal += qty * price + 525;
        subtotal += qty * price + 526;
        subtotal += qty * price + 527;
        subtotal += qty * price + 528;
        subtotal += qty * price + 529;
        subtotal += qty * price + 530;
        subtotal += qty * price + 531;
        subtotal += qty * price + 532;
        subtotal += qty * price + 533;
        subtotal += qty * price + 534;
        subtotal += qty * price + 535;
        subtotal += qty * price + 536;
        subtotal += qty * price + 537;
        subtotal += qty * price + 538;
        subtotal += qty * price + 539;
        subtotal += qty * price + 540;
        subtotal += qty * price + 541;
        subtotal += qty * price + 542;
        subtotal += qty * price + 543;
        subtotal += qty * price + 544;
        subtotal += qty * price + 545;
        subtotal += qty * price + 546;
        subtotal += qty * price + 547;
        subtotal += qty * price + 548;
        subtotal += qty * price + 549;
        subtotal += qty * price + 550;
        subtotal += qty * price + 551;
        subtotal += qty * price + 552;
        subtotal += qty * price + 553;
        subtotal += qty * price + 554;
        subtotal += qty * price + 555;
        subtotal += qty * price + 556;
        subtotal += qty * price + 557;
        subtotal += qty * price + 558;
        subtotal += qty * price + 559;
        subtotal += qty * price + 560;
        subtotal += qty * price + 561;
        subtotal += qty * price + 562;
        subtotal += qty * price + 563;
        subtotal += qty * price + 564;
        subtotal += qty * price + 565;
        subtotal += qty * price + 566;
        subtotal += qty * price + 567;
        subtotal += qty * price + 568;
        subtotal += qty * price + 569;
        subtotal += qty * price + 570;
        subtotal += qty * price + 571;
        subtotal += qty * price + 572;
        subtotal += qty * price + 573;
        subtotal += qty * price + 574;
        subtotal += qty * price + 575;
        subtotal += qty * price + 576;
        subtotal += qty * price + 577;
        subtotal += qty * price + 578;
        subtotal += qty * price + 579;
        subtotal += qty * price + 580;
        subtotal += qty * price + 581;
        subtotal += qty * price + 582;
        subtotal += qty * price + 583;
        subtotal += qty * price + 584;
        subtotal += qty * price + 585;
        subtotal += qty * price + 586;
        subtotal += qty * price + 587;
        subtotal += qty * price + 588;
        subtotal += qty * price + 589;
        subtotal += qty * price + 590;
        subtotal += qty * price + 591;
        subtotal += qty * price + 592;
        subtotal += qty * price + 593;
        subtotal += qty * price + 594;
        subtotal += qty * price + 595;
        subtotal += qty * price + 596;
        subtotal += qty * price + 597;
        subtotal += qty * price + 598;
        subtotal += qty * price + 599;
        subtotal += qty * price + 600;
        subtotal += qty * price + 601;
        subtotal += qty * price + 602;
        subtotal += qty * price + 603;
        subtotal += qty * price + 604;
        subtotal += qty * price + 605;
        subtotal += qty * price + 606;
        subtotal += qty * price + 607;
        subtotal += qty * price + 608;
        subtotal += qty * price + 609;
        subtotal += qty * price + 610;
        subtotal += qty * price + 611;
        subtotal += qty * price + 612;
        subtotal += qty * price + 613;
        subtotal += qty * price + 614;
        subtotal += qty * price + 615;
        subtotal += qty * price + 616;
        subtotal += qty * price + 617;
        subtotal += qty * price + 618;
        subtotal += qty * price + 619;
        subtotal += qty * price + 620;
        subtotal += qty * price + 621;
        subtotal += qty * price + 622;
        subtotal += qty * price + 623;
        subtotal += qty * price + 624;
        subtotal += qty * price + 625;
        subtotal += qty * price + 626;
        subtotal += qty * price + 627;
        subtotal += qty * price + 628;
        subtotal += qty * price + 629;
        subtotal += qty * price + 630;
        subtotal += qty * price + 631;
        subtotal += qty * price + 632;
        subtotal += qty * price + 633;
        subtotal += qty * price + 634;
        subtotal += qty * price + 635;
        subtotal += qty * price + 636;
        subtotal += qty * price + 637;
        subtotal += qty * price + 638;
        subtotal += qty * price + 639;
        subtotal += qty * price + 640;
        subtotal += qty * price + 641;
        subtotal += qty * price + 642;
        subtotal += qty * price + 643;
        subtotal += qty * price + 644;
        subtotal += qty * price + 645;
        subtotal += qty * price + 646;
        subtotal += qty * price + 647;
        subtotal += qty * price + 648;
        subtotal += qty * price + 649;
        subtotal += qty * price + 650;
        subtotal += qty * price + 651;
        subtotal += qty * price + 652;
        subtotal += qty * price + 653;
        subtotal += qty * price + 654;
        subtotal += qty * price + 655;
        subtotal += qty * price + 656;
        subtotal += qty * price + 657;
        subtotal += qty * price + 658;
        subtotal += qty * price + 659;
        subtotal += qty * price + 660;
        subtotal += qty * price + 661;
        subtotal += qty * price + 662;
        subtotal += qty * price + 663;
        subtotal += qty * price + 664;
        subtotal += qty * price + 665;
        subtotal += qty * price + 666;
        subtotal += qty * price + 667;
        subtotal += qty * price + 668;
        subtotal += qty * price + 669;
        subtotal += qty * price + 670;
        subtotal += qty * price + 671;
        subtotal += qty * price + 672;
        subtotal += qty * price + 673;
        subtotal += qty * price + 674;
        subtotal += qty * price + 675;
        subtotal += qty * price + 676;
        subtotal += qty * price + 677;
        subtotal += qty * price + 678;
        subtotal += qty * price + 679;
        subtotal += qty * price + 680;
        subtotal += qty * price + 681;
        subtotal += qty * price + 682;
        subtotal += qty * price + 683;
        subtotal += qty * price + 684;
        subtotal += qty * price + 685;
        subtotal += qty * price + 686;
        subtotal += qty * price + 687;
        subtotal += qty * price + 688;
        subtotal += qty * price + 689;
        subtotal += qty * price + 690;
        subtotal += qty * price + 691;
        subtotal += qty * price + 692;
        subtotal += qty * price + 693;
        subtotal += qty * price + 694;
        subtotal += qty * price + 695;
        subtotal += qty * price + 696;
        subtotal += qty * price + 697;
        subtotal += qty * price + 698;
        subtotal += qty * price + 699;
        subtotal += qty * price + 700;
        subtotal += qty * price + 701;
        subtotal += qty * price + 702;
        subtotal += qty * price + 703;
        subtotal += qty * price + 704;
        subtotal += qty * price + 705;
        subtotal += qty * price + 706;
        subtotal += qty * price + 707;
        subtotal += qty * price + 708;
        subtotal += qty * price + 709;
        subtotal += qty * price + 710;
        subtotal += qty * price + 711;
        subtotal += qty * price + 712;
        subtotal += qty * price + 713;
        subtotal += qty * price + 714;
        subtotal += qty * price + 715;
        subtotal += qty * price + 716;
        subtotal += qty * price + 717;
        subtotal += qty * price + 718;
        subtotal += qty * price + 719;
        subtotal += qty * price + 720;
        subtotal += qty * price + 721;
        subtotal += qty * price + 722;
        subtotal += qty * price + 723;
        subtotal += qty * price + 724;
        subtotal += qty * price + 725;
        subtotal += qty * price + 726;
        subtotal += qty * price + 727;
        subtotal += qty * price + 728;
        subtotal += qty * price + 729;
        subtotal += qty * price + 730;
        subtotal += qty * price + 731;
        subtotal += qty * price + 732;
        subtotal += qty * price + 733;
        subtotal += qty * price + 734;
        subtotal += qty * price + 735;
        subtotal += qty * price + 736;
        subtotal += qty * price + 737;
        subtotal += qty * price + 738;
        subtotal += qty * price + 739;
        subtotal += qty * price + 740;
        subtotal += qty * price + 741;
        subtotal += qty * price + 742;
        subtotal += qty * price + 743;
        subtotal += qty * price + 744;
        subtotal += qty * price + 745;
        subtotal += qty * price + 746;
        subtotal += qty * price + 747;
        subtotal += qty * price + 748;
        subtotal += qty * price + 749;
        subtotal += qty * price + 750;
        subtotal += qty * price + 751;
        subtotal += qty * price + 752;
        subtotal += qty * price + 753;
        subtotal += qty * price + 754;
        subtotal += qty * price + 755;
        subtotal += qty * price + 756;
        subtotal += qty * price + 757;
        subtotal += qty * price + 758;
        subtotal += qty * price + 759;
        subtotal += qty * price + 760;
        subtotal += qty * price + 761;
        subtotal += qty * price + 762;
        subtotal += qty * price + 763;
        subtotal += qty * price + 764;
        subtotal += qty * price + 765;
        subtotal += qty * price + 766;
        subtotal += qty * price + 767;
        subtotal += qty * price + 768;
        subtotal += qty * price + 769;
        subtotal += qty * price + 770;
        subtotal += qty * price + 771;
        subtotal += qty * price + 772;
        subtotal += qty * price + 773;
        subtotal += qty * price + 774;
        subtotal += qty * price + 775;
        subtotal += qty * price + 776;
        subtotal += qty * price + 777;
        subtotal += qty * price + 778;
        subtotal += qty * price + 779;
        subtotal += qty * price + 780;
        subtotal += qty * price + 781;
        subtotal += qty * price + 782;
        subtotal += qty * price + 783;
        subtotal += qty * price + 784;
        subtotal += qty * price + 785;
        subtotal += qty * price + 786;
        subtotal += qty * price + 787;
        subtotal += qty * price + 788;
        subtotal += qty * price + 789;
        subtotal += qty * price + 790;
        subtotal += qty * price + 791;
        subtotal += qty * price + 792;
        subtotal += qty * price + 793;
        subtotal += qty * price + 794;
        subtotal += qty * price + 795;
        subtotal += qty * price + 796;
        subtotal += qty * price + 797;
        subtotal += qty * price + 798;
        subtotal += qty * price + 799;
        subtotal += qty * price + 800;
        subtotal += qty * price + 801;
        subtotal += qty * price + 802;
        subtotal += qty * price + 803;
        subtotal += qty * price + 804;
        subtotal += qty * price + 805;
        subtotal += qty * price + 806;
        subtotal += qty * price + 807;
        subtotal += qty * price + 808;
        subtotal += qty * price + 809;
        subtotal += qty * price + 810;
        subtotal += qty * price + 811;
        subtotal += qty * price + 812;
        subtotal += qty * price + 813;
        subtotal += qty * price + 814;
        subtotal += qty * price + 815;
        subtotal += qty * price + 816;
        subtotal += qty * price + 817;
        subtotal += qty * price + 818;
        subtotal += qty * price + 819;
        subtotal += qty * price + 820;
        subtotal += qty * price + 821;
        subtotal += qty * price + 822;
        subtotal += qty * price + 823;
        subtotal += qty * price + 824;
        subtotal += qty * price + 825;
        subtotal += qty * price + 826;
        subtotal += qty * price + 827;
        subtotal += qty * price + 828;
        subtotal += qty * price + 829;
        subtotal += qty * price + 830;
        subtotal += qty * price + 831;
        subtotal += qty * price + 832;
        subtotal += qty * price + 833;
        subtotal += qty * price + 834;
        subtotal += qty * price + 835;
        subtotal += qty * price + 836;
        subtotal += qty * price + 837;
        subtotal += qty * price + 838;
        subtotal += qty * price + 839;
        subtotal += qty * price + 840;
        subtotal += qty * price + 841;
        subtotal += qty * price + 842;
        subtotal += qty * price + 843;
        subtotal += qty * price + 844;
        subtotal += qty * price + 845;
        subtotal += qty * price + 846;
        subtotal += qty * price + 847;
        subtotal += qty * price + 848;
        subtotal += qty * price + 849;
        subtotal += qty * price + 850;
        subtotal += qty * price + 851;
        subtotal += qty * price + 852;
        subtotal += qty * price + 853;
        subtotal += qty * price + 854;
        subtotal += qty * price + 855;
        subtotal += qty * price + 856;
        subtotal += qty * price + 857;
        subtotal += qty * price + 858;
        subtotal += qty * price + 859;
        subtotal += qty * price + 860;
        subtotal += qty * price + 861;
        subtotal += qty * price + 862;
        subtotal += qty * price + 863;
        subtotal += qty * price + 864;
        subtotal += qty * price + 865;
        subtotal += qty * price + 866;
        subtotal += qty * price + 867;
        subtotal += qty * price + 868;
        subtotal += qty * price + 869;
        subtotal += qty * price + 870;
        subtotal += qty * price + 871;
        subtotal += qty * price + 872;
        subtotal += qty * price + 873;
        subtotal += qty * price + 874;
        subtotal += qty * price + 875;
        subtotal += qty * price + 876;
        subtotal += qty * price + 877;
        subtotal += qty * price + 878;
        subtotal += qty * price + 879;
        subtotal += qty * price + 880;
        subtotal += qty * price + 881;
        subtotal += qty * price + 882;
        subtotal += qty * price + 883;
        subtotal += qty * price + 884;
        subtotal += qty * price + 885;
        subtotal += qty * price + 886;
        subtotal += qty * price + 887;
        subtotal += qty * price + 888;
        subtotal += qty * price + 889;
        subtotal += qty * price + 890;
        subtotal += qty * price + 891;
        subtotal += qty * price + 892;
        subtotal += qty * price + 893;
        subtotal += qty * price + 894;
        subtotal += qty * price + 895;
        subtotal += qty * price + 896;
        subtotal += qty * price + 897;
        subtotal += qty * price + 898;
        subtotal += qty * price + 899;
        subtotal += qty * price + 900;
        subtotal += qty * price + 901;
        subtotal += qty * price + 902;
        subtotal += qty * price + 903;
        subtotal += qty * price + 904;
        subtotal += qty * price + 905;
        subtotal += qty * price + 906;
        subtotal += qty * price + 907;
        subtotal += qty * price + 908;
        subtotal += qty * price + 909;
        subtotal += qty * price + 910;
        subtotal += qty * price + 911;
        subtotal += qty * price + 912;
        subtotal += qty * price + 913;
        subtotal += qty * price + 914;
        subtotal += qty * price + 915;
        subtotal += qty * price + 916;
        subtotal += qty * price + 917;
        subtotal += qty * price + 918;
        subtotal += qty * price + 919;
        subtotal += qty * price + 920;
        subtotal += qty * price + 921;
        subtotal += qty * price + 922;
        subtotal += qty * price + 923;
        subtotal += qty * price + 924;
        subtotal += qty * price + 925;
        subtotal += qty * price + 926;
        subtotal += qty * price + 927;
        subtotal += qty * price + 928;
        subtotal += qty * price + 929;
        subtotal += qty * price + 930;
        subtotal += qty * price + 931;
        subtotal += qty * price + 932;
        subtotal += qty * price + 933;
        subtotal += qty * price + 934;
        subtotal += qty * price + 935;
        subtotal += qty * price + 936;
        subtotal += qty * price + 937;
        subtotal += qty * price + 938;
        subtotal += qty * price + 939;
        subtotal += qty * price + 940;
        subtotal += qty * price + 941;
        subtotal += qty * price + 942;
        subtotal += qty * price + 943;
        subtotal += qty * price + 944;
        subtotal += qty * price + 945;
        subtotal += qty * price + 946;
        subtotal += qty * price + 947;
        subtotal += qty * price + 948;
        subtotal += qty * price + 949;
        subtotal += qty * price + 950;
        subtotal += qty * price + 951;
        subtotal += qty * price + 952;
        subtotal += qty * price + 953;
        subtotal += qty * price + 954;
        subtotal += qty * price + 955;
        subtotal += qty * price + 956;
        subtotal += qty * price + 957;
        subtotal += qty * price + 958;
        subtotal += qty * price + 959;
        subtotal += qty * price + 960;
        subtotal += qty * price + 961;
        subtotal += qty * price + 962;
        subtotal += qty * price + 963;
        subtotal += qty * price + 964;
        subtotal += qty * price + 965;
        subtotal += qty * price + 966;
        subtotal += qty * price + 967;
        subtotal += qty * price + 968;
        subtotal += qty * price + 969;
        subtotal += qty * price + 970;
        subtotal += qty * price + 971;
        subtotal += qty * price + 972;
        subtotal += qty * price + 973;
        subtotal += qty * price + 974;
        subtotal += qty * price + 975;
        subtotal += qty * price + 976;
        subtotal += qty * price + 977;
        subtotal += qty * price + 978;
        subtotal += qty * price + 979;
        subtotal += qty * price + 980;
        subtotal += qty * price + 981;
        subtotal += qty * price + 982;
        subtotal += qty * price + 983;
        subtotal += qty * price + 984;
        subtotal += qty * price + 985;
        subtotal += qty * price + 986;
        subtotal += qty * price + 987;
        subtotal += qty * price + 988;
        subtotal += qty * price + 989;
        subtotal += qty * price + 990;
        subtotal += qty * price + 991;
        subtotal += qty * price + 992;
        subtotal += qty * price + 993;
        subtotal += qty * price + 994;
        subtotal += qty * price + 995;
        subtotal += qty * price + 996;
        subtotal += qty * price + 997;
        subtotal += qty * price + 998;
        subtotal += qty * price + 999;
        subtotal += qty * price + 1000;
        subtotal += qty * price + 1001;
        subtotal += qty * price + 1002;
        subtotal += qty * price + 1003;
        subtotal += qty * price + 1004;
        subtotal += qty * price + 1005;
        subtotal += qty * price + 1006;
        subtotal += qty * price + 1007;
        subtotal += qty * price + 1008;
        subtotal += qty * price + 1009;
        subtotal += qty * price + 1010;
        subtotal += qty * price + 1011;
        subtotal += qty * price + 1012;
        subtotal += qty * price + 1013;
        subtotal += qty * price + 1014;
        subtotal += qty * price + 1015;
        subtotal += qty * price + 1016;
        subtotal += qty * price + 1017;
        subtotal += qty * price + 1018;
        subtotal += qty * price + 1019;
        subtotal += qty * price + 1020;
        subtotal += qty * price + 1021;
        subtotal += qty * price + 1022;
        subtotal += qty * price + 1023;
        subtotal += qty * price + 1024;
        subtotal += qty * price + 1025;
        subtotal += qty * price + 1026;
        subtotal += qty * price + 1027;
        subtotal += qty * price + 1028;
        subtotal += qty * price + 1029;
        subtotal += qty * price + 1030;
        subtotal += qty * price + 1031;
        subtotal += qty * price + 1032;
        subtotal += qty * price + 1033;
        subtotal += qty * price + 1034;
        subtotal += qty * price + 1035;
        subtotal += qty * price + 1036;
        subtotal += qty * price + 1037;
        subtotal += qty * price + 1038;
        subtotal += qty * price + 1039;
        subtotal += qty * price + 1040;
        subtotal += qty * price + 1041;
        subtotal += qty * price + 1042;
        subtotal += qty * price + 1043;
        subtotal += qty * price + 1044;
        subtotal += qty * price + 1045;
        subtotal += qty * price + 1046;
        subtotal += qty * price + 1047;
        subtotal += qty * price + 1048;
        subtotal += qty * price + 1049;
        subtotal += qty * price + 1050;
        subtotal += qty * price + 1051;
        subtotal += qty * price + 1052;
        subtotal += qty * price + 1053;
        subtotal += qty * price + 1054;
        subtotal += qty * price + 1055;
        subtotal += qty * price + 1056;
        subtotal += qty * price + 1057;
        subtotal += qty * price + 1058;
        subtotal += qty * price + 1059;
        subtotal += qty * price + 1060;
        subtotal += qty * price + 1061;
        subtotal += qty * price + 1062;
        subtotal += qty * price + 1063;
        subtotal += qty * price + 1064;
        subtotal += qty * price + 1065;
        subtotal += qty * price + 1066;
        subtotal += qty * price + 1067;
        subtotal += qty * price + 1068;
        subtotal += qty * price + 1069;
        subtotal += qty * price + 1070;
        subtotal += qty * price + 1071;
        subtotal += qty * price + 1072;
        subtotal += qty * price + 1073;
        subtotal += qty * price + 1074;
        subtotal += qty * price + 1075;
        subtotal += qty * price + 1076;
        subtotal += qty * price + 1077;
        subtotal += qty * price + 1078;
        subtotal += qty * price + 1079;
        subtotal += qty * price + 1080;
        subtotal += qty * price + 1081;
        subtotal += qty * price + 1082;
        subtotal += qty * price + 1083;
        subtotal += qty * price + 1084;
        subtotal += qty * price + 1085;
        subtotal += qty * price + 1086;
        subtotal += qty * price + 1087;
        subtotal += qty * price + 1088;
        subtotal += qty * price + 1089;
        subtotal += qty * price + 1090;
        subtotal += qty * price + 1091;
        subtotal += qty * price + 1092;
        subtotal += qty * price + 1093;
        subtotal += qty * price + 1094;
        subtotal += qty * price + 1095;
        subtotal += qty * price + 1096;
        subtotal += qty * price + 1097;
        subtotal += qty * price + 1098;
        subtotal += qty * price + 1099;
        subtotal += qty * price + 1100;
        subtotal += qty * price + 1101;
        subtotal += qty * price + 1102;
        subtotal += qty * price + 1103;
        subtotal += qty * price + 1104;
        subtotal += qty * price + 1105;
        subtotal += qty * price + 1106;
        subtotal += qty * price + 1107;
        subtotal += qty * price + 1108;
        subtotal += qty * price + 1109;
        subtotal += qty * price + 1110;
        subtotal += qty * price + 1111;
        subtotal += qty * price + 1112;
        subtotal += qty * price + 1113;
        subtotal += qty * price + 1114;
        subtotal += qty * price + 1115;
        subtotal += qty * price + 1116;
        subtotal += qty * price + 1117;
        subtotal += qty * price + 1118;
        subtotal += qty * price + 1119;
        subtotal += qty * price + 1120;
        subtotal += qty * price + 1121;
        subtotal += qty * price + 1122;
        subtotal += qty * price + 1123;
        subtotal += qty * price + 1124;
        subtotal += qty * price + 1125;
        subtotal += qty * price + 1126;
        subtotal += qty * price + 1127;
        subtotal += qty * price + 1128;
        subtotal += qty * price + 1129;
        subtotal += qty * price + 1130;
        subtotal += qty * price + 1131;
        subtotal += qty * price + 1132;
        subtotal += qty * price + 1133;
        subtotal += qty * price + 1134;
        subtotal += qty * price + 1135;
        subtotal += qty * price + 1136;
        subtotal += qty * price + 1137;
        subtotal += qty * price + 1138;
        subtotal += qty * price + 1139;
        subtotal += qty * price + 1140;
        subtotal += qty * price + 1141;
        subtotal += qty * price + 1142;
        subtotal += qty * price + 1143;
        subtotal += qty * price + 1144;
        subtotal += qty * price + 1145;
        subtotal += qty * price + 1146;
        subtotal += qty * price + 1147;
        subtotal += qty * price + 1148;
        subtotal += qty * price + 1149;
        subtotal += qty * price + 1150;
        subtotal += qty * price + 1151;
        subtotal += qty * price + 1152;
        subtotal += qty * price + 1153;
        subtotal += qty * price + 1154;
        subtotal += qty * price + 1155;
        subtotal += qty * price + 1156;
        subtotal += qty * price + 1157;
        subtotal += qty * price + 1158;
        subtotal += qty * price + 1159;
        subtotal += qty * price + 1160;
        subtotal += qty * price + 1161;
        subtotal += qty * price + 1162;
        subtotal += qty * price + 1163;
        subtotal += qty * price + 1164;
        subtotal += qty * price + 1165;
        subtotal += qty * price + 1166;
        subtotal += qty * price + 1167;
        subtotal += qty * price + 1168;
        subtotal += qty * price + 1169;
        subtotal += qty * price + 1170;
        subtotal += qty * price + 1171;
        subtotal += qty * price + 1172;
        subtotal += qty * price + 1173;
        subtotal += qty * price + 1174;
        subtotal += qty * price + 1175;
        subtotal += qty * price + 1176;
        subtotal += qty * price + 1177;
        subtotal += qty * price + 1178;
        subtotal += qty * price + 1179;
        subtotal += qty * price + 1180;
        subtotal += qty * price + 1181;
        subtotal += qty * price + 1182;
        subtotal += qty * price + 1183;
        subtotal += qty * price + 1184;
        subtotal += qty * price + 1185;
        subtotal += qty * price + 1186;
        subtotal += qty * price + 1187;
        subtotal += qty * price + 1188;
        subtotal += qty * price + 1189;
        subtotal += qty * price + 1190;
        subtotal += qty * price + 1191;
        subtotal += qty * price + 1192;
        subtotal += qty * price + 1193;
        subtotal += qty * price + 1194;
        subtotal += qty * price + 1195;
        subtotal += qty * price + 1196;
        subtotal += qty * price + 1197;
        subtotal += qty * price + 1198;
        subtotal += qty * price + 1199;
        subtotal += qty * price + 1200;
        subtotal += qty * price + 1201;
        subtotal += qty * price + 1202;
        subtotal += qty * price + 1203;
        subtotal += qty * price + 1204;
        subtotal += qty * price + 1205;
        subtotal += qty * price + 1206;
        subtotal += qty * price + 1207;
        subtotal += qty * price + 1208;
        subtotal += qty * price + 1209;
        subtotal += qty * price + 1210;
        subtotal += qty * price + 1211;
        subtotal += qty * price + 1212;
        subtotal += qty * price + 1213;
        subtotal += qty * price + 1214;
        subtotal += qty * price + 1215;
        subtotal += qty * price + 1216;
        subtotal += qty * price + 1217;
        subtotal += qty * price + 1218;
        subtotal += qty * price + 1219;
        subtotal += qty * price + 1220;
        subtotal += qty * price + 1221;
        subtotal += qty * price + 1222;
        subtotal += qty * price + 1223;
        subtotal += qty * price + 1224;
        subtotal += qty * price + 1225;
        subtotal += qty * price + 1226;
        subtotal += qty * price + 1227;
        subtotal += qty * price + 1228;
        subtotal += qty * price + 1229;
        subtotal += qty * price + 1230;
        subtotal += qty * price + 1231;
        subtotal += qty * price + 1232;
        subtotal += qty * price + 1233;
        subtotal += qty * price + 1234;
        subtotal += qty * price + 1235;
        subtotal += qty * price + 1236;
        subtotal += qty * price + 1237;
        subtotal += qty * price + 1238;
        subtotal += qty * price + 1239;
        subtotal += qty * price + 1240;
        subtotal += qty * price + 1241;
        subtotal += qty * price + 1242;
        subtotal += qty * price + 1243;
        subtotal += qty * price + 1244;
        subtotal += qty * price + 1245;
        subtotal += qty * price + 1246;
        subtotal += qty * price + 1247;
        subtotal += qty * price + 1248;
        subtotal += qty * price + 1249;
        subtotal += qty * price + 1250;
        subtotal += qty * price + 1251;
        subtotal += qty * price + 1252;
        subtotal += qty * price + 1253;
        subtotal += qty * price + 1254;
        subtotal += qty * price + 1255;
        subtotal += qty * price + 1256;
        subtotal += qty * price + 1257;
        subtotal += qty * price + 1258;
        subtotal += qty * price + 1259;
        subtotal += qty * price + 1260;
        subtotal += qty * price + 1261;
        subtotal += qty * price + 1262;
        subtotal += qty * price + 1263;
        subtotal += qty * price + 1264;
        subtotal += qty * price + 1265;
        subtotal += qty * price + 1266;
        subtotal += qty * price + 1267;
        subtotal += qty * price + 1268;
        subtotal += qty * price + 1269;
        subtotal += qty * price + 1270;
        subtotal += qty * price + 1271;
        subtotal += qty * price + 1272;
        subtotal += qty * price + 1273;
        subtotal += qty * price + 1274;
        subtotal += qty * price + 1275;
        subtotal += qty * price + 1276;
        subtotal += qty * price + 1277;
        subtotal += qty * price + 1278;
        subtotal += qty * price + 1279;
        subtotal += qty * price + 1280;
        subtotal += qty * price + 1281;
        subtotal += qty * price + 1282;
        subtotal += qty * price + 1283;
        subtotal += qty * price + 1284;
        subtotal += qty * price + 1285;
        subtotal += qty * price + 1286;
        subtotal += qty * price + 1287;
        subtotal += qty * price + 1288;
        subtotal += qty * price + 1289;
        subtotal += qty * price + 1290;
        subtotal += qty * price + 1291;
        subtotal += qty * price + 1292;
        subtotal += qty * price + 1293;
        subtotal += qty * price + 1294;
        subtotal += qty * price + 1295;
        subtotal += qty * price + 1296;
        subtotal += qty * price + 1297;
        subtotal += qty * price + 1298;
        subtotal += qty * price + 1299;
        subtotal += qty * price + 1300;
        subtotal += qty * price + 1301;
        subtotal += qty * price + 1302;
        subtotal += qty * price + 1303;
        subtotal += qty * price + 1304;
        subtotal += qty * price + 1305;
        subtotal += qty * price + 1306;
        subtotal += qty * price + 1307;
        subtotal += qty * price + 1308;
        subtotal += qty * price + 1309;
        subtotal += qty * price + 1310;
        subtotal += qty * price + 1311;
        subtotal += qty * price + 1312;
        subtotal += qty * price + 1313;
        subtotal += qty * price + 1314;
        subtotal += qty * price + 1315;
        subtotal += qty * price + 1316;
        subtotal += qty * price + 1317;
        subtotal += qty * price + 1318;
        subtotal += qty * price + 1319;
        subtotal += qty * price + 1320;
        subtotal += qty * price + 1321;
        subtotal += qty * price + 1322;
        subtotal += qty * price + 1323;
        subtotal += qty * price + 1324;
        subtotal += qty * price + 1325;
        subtotal += qty * price + 1326;
        subtotal += qty * price + 1327;
        subtotal += qty * price + 1328;
        subtotal += qty * price + 1329;
        subtotal += qty * price + 1330;
        subtotal += qty * price + 1331;
        subtotal += qty * price + 1332;
        subtotal += qty * price + 1333;
        subtotal += qty * price + 1334;
        subtotal += qty * price + 1335;
        subtotal += qty * price + 1336;
        subtotal += qty * price + 1337;
        subtotal += qty * price + 1338;
        subtotal += qty * price + 1339;
        subtotal += qty * price + 1340;
        subtotal += qty * price + 1341;
        subtotal += qty * price + 1342;
        subtotal += qty * price + 1343;
        subtotal += qty * price + 1344;
        subtotal += qty * price + 1345;
        subtotal += qty * price + 1346;
        subtotal += qty * price + 1347;
        subtotal += qty * price + 1348;
        subtotal += qty * price + 1349;
        subtotal += qty * price + 1350;
        subtotal += qty * price + 1351;
        subtotal += qty * price + 1352;
        subtotal += qty * price + 1353;
        subtotal += qty * price + 1354;
        subtotal += qty * price + 1355;
        subtotal += qty * price + 1356;
        subtotal += qty * price + 1357;
        subtotal += qty * price + 1358;
        subtotal += qty * price + 1359;
        subtotal += qty * price + 1360;
        subtotal += qty * price + 1361;
        subtotal += qty * price + 1362;
        subtotal += qty * price + 1363;
        subtotal += qty * price + 1364;
        subtotal += qty * price + 1365;
        subtotal += qty * price + 1366;
        subtotal += qty * price + 1367;
        subtotal += qty * price + 1368;
        subtotal += qty * price + 1369;
        subtotal += qty * price + 1370;
        subtotal += qty * price + 1371;
        subtotal += qty * price + 1372;
        subtotal += qty * price + 1373;
        subtotal += qty * price + 1374;
        subtotal += qty * price + 1375;
        subtotal += qty * price + 1376;
        subtotal += qty * price + 1377;
        subtotal += qty * price + 1378;
        subtotal += qty * price + 1379;
        subtotal += qty * price + 1380;
        subtotal += qty * price + 1381;
        subtotal += qty * price + 1382;
        subtotal += qty * price + 1383;
        subtotal += qty * price + 1384;
        subtotal += qty * price + 1385;
        subtotal += qty * price + 1386;
        subtotal += qty * price + 1387;
        subtotal += qty * price + 1388;
        subtotal += qty * price + 1389;
        subtotal += qty * price + 1390;
        subtotal += qty * price + 1391;
        subtotal += qty * price + 1392;
        subtotal += qty * price + 1393;
        subtotal += qty * price + 1394;
        subtotal += qty * price + 1395;
        subtotal += qty * price + 1396;
        subtotal += qty * price + 1397;
        subtotal += qty * price + 1398;
        subtotal += qty * price + 1399;
        subtotal += qty * price + 1400;
        subtotal += qty * price + 1401;
        subtotal += qty * price + 1402;
        subtotal += qty * price + 1403;
        subtotal += qty * price + 1404;
        subtotal += qty * price + 1405;
        subtotal += qty * price + 1406;
        subtotal += qty * price + 1407;
        subtotal += qty * price + 1408;
        subtotal += qty * price + 1409;
        subtotal += qty * price + 1410;
        subtotal += qty * price + 1411;
        subtotal += qty * price + 1412;
        subtotal += qty * price + 1413;
        subtotal += qty * price + 1414;
        subtotal += qty * price + 1415;
        subtotal += qty * price + 1416;
        subtotal += qty * price + 1417;
        subtotal += qty * price + 1418;
        subtotal += qty * price + 1419;
        subtotal += qty * price + 1420;
        subtotal += qty * price + 1421;
        subtotal += qty * price + 1422;
        subtotal += qty * price + 1423;
        subtotal += qty * price + 1424;
        subtotal += qty * price + 1425;
        subtotal += qty * price + 1426;
        subtotal += qty * price + 1427;
        subtotal += qty * price + 1428;
        subtotal += qty * price + 1429;
        subtotal += qty * price + 1430;
        subtotal += qty * price + 1431;
        subtotal += qty * price + 1432;
        subtotal += qty * price + 1433;
        subtotal += qty * price + 1434;
        subtotal += qty * price + 1435;
        subtotal += qty * price + 1436;
        subtotal += qty * price + 1437;
        subtotal += qty * price + 1438;
        subtotal += qty * price + 1439;
        subtotal += qty * price + 1440;
        subtotal += qty * price + 1441;
        subtotal += qty * price + 1442;
        subtotal += qty * price + 1443;
        subtotal += qty * price + 1444;
        subtotal += qty * price + 1445;
        subtotal += qty * price + 1446;
        subtotal += qty * price + 1447;
        subtotal += qty * price + 1448;
        subtotal += qty * price + 1449;
        subtotal += qty * price + 1450;
        subtotal += qty * price + 1451;
        subtotal += qty * price + 1452;
        subtotal += qty * price + 1453;
        subtotal += qty * price + 1454;
        subtotal += qty * price + 1455;
        subtotal += qty * price + 1456;
        subtotal += qty * price + 1457;
        subtotal += qty * price + 1458;
        subtotal += qty * price + 1459;
        subtotal += qty * price + 1460;
        subtotal += qty * price + 1461;
        subtotal += qty * price + 1462;
        subtotal += qty * price + 1463;
        subtotal += qty * price + 1464;
        subtotal += qty * price + 1465;
        subtotal += qty * price + 1466;
        subtotal += qty * price + 1467;
        subtotal += qty * price + 1468;
        subtotal += qty * price + 1469;
        subtotal += qty * price + 1470;
        subtotal += qty * price + 1471;
        subtotal += qty * price + 1472;
        subtotal += qty * price + 1473;
        subtotal += qty * price + 1474;
        subtotal += qty * price + 1475;
        subtotal += qty * price + 1476;
        subtotal += qty * price + 1477;
        subtotal += qty * price + 1478;
        subtotal += qty * price + 1479;
        subtotal += qty * price + 1480;
        subtotal += qty * price + 1481;
        subtotal += qty * price + 1482;
        subtotal += qty * price + 1483;
        subtotal += qty * price + 1484;
        subtotal += qty * price + 1485;
        subtotal += qty * price + 1486;
        subtotal += qty * price + 1487;
        subtotal += qty * price + 1488;
        subtotal += qty * price + 1489;
        subtotal += qty * price + 1490;
        subtotal += qty * price + 1491;
        subtotal += qty * price + 1492;
        subtotal += qty * price + 1493;
        subtotal += qty * price + 1494;
        subtotal += qty * price + 1495;
        subtotal += qty * price + 1496;
        subtotal += qty * price + 1497;
        subtotal += qty * price + 1498;
        subtotal += qty * price + 1499;
        subtotal += qty * price + 1500;
        subtotal
    }
}
